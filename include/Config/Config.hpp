#ifndef WEBSERV_CONFIG_HPP
#define WEBSERV_CONFIG_HPP

#include <vector>
#include <map>
#include <set>
#include <string>

// from the config i want to get servers by: name, ip, ip:port, port
// from the config i want to get directives by: name
// -+
// from server i want to get locations by: arg
// form server i want to get directives by: name
// -+
// from the locations i want to get directives by: name

class Config
{
public:
};

#endif